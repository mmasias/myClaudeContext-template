# System repo manifest
# Format: <github-url>   <path-relative-to-~/misRepos/>
# Example:
# https://github.com/user/my-project.git   proyectos/my-project

# Add repos here. bootstrap.sh will clone them automatically on new machines.
